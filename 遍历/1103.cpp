#include <cmath>
#include <cstdio>
#include <iostream>
using namespace std;
int main()
{
    int n, k, p;
    cin >> n >> k >> p;
    int num = pow(n, 1.0 / p);
    for (int i = num; i > 0; i++)
    {
        
    }
    return 0;
}